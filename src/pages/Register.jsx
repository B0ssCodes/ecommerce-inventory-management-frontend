import React from "react";
import { Typography } from "antd";
const { Title } = Typography;
function Register() {
  return (
    <div>
      <Title type={1} style={{ textAlign: "center" }}>
        Register
      </Title>
    </div>
  );
}

export default Register;
